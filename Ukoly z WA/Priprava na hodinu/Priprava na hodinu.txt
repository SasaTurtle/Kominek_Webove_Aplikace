1) Když je atribut action prázdný, data z formuláře budou odeslána zpět na aktuální stránku.
2)Atribut required funguje i na <input type="radio">. Pokud je na něm nastavený, uživatel bude muset vybrat jednu z možností radio buttonů, nebo jinak nebude formulář povolený k odeslání.
3)HTML entity jsou speciální kódy používané v HTML kódu pro reprezentaci znaků, které mají speciální význam v HTML (například < by se mělo zapsat jako &lt;, > jako &gt;, atd.). Tyto entity se používají pro zabezpečení a správnou interpretaci znaků v HTML dokumentech, zejména když se jedná o znaky, které by mohly být chápány jako součást HTML kódu (jako < nebo &).
4)UNICODE znaky se kódují do URL pomocí procesu nazývaného "percent encoding" (též URL encoding). Každý znak je převeden na sérii procentových znaků (%), následovanou dvěma hexadecimálními číslicemi, které reprezentují jeho UNICODE hodnotu. Například znak "😀" by byl zakódován jako "%F0%9F%98%80" v URL.
5)V URL se používají znakové entity (např. &amp; pro &) místo samotného znaku &, protože & má speciální význam v URL jako oddělovač parametrů (query string) a může způsobit problémy, pokud je použit v obsahu neoznačeném jako parametr (např. v textu stránky). Použitím entity se zajistí, že & bude interpretováno jako běžný znak, nikoli jako součást URL syntaxe.